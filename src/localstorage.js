// const body = document.querySelector("body");

// const Theme = {
//   LIGHT: "light-theme",
//   DARK: "dark-theme",
// };

// getColorThemeLocalStorage();

// const STORAGE_KEY = "Theme";

// function getColorThemeLocalStorage() {
//   const themeStatus = localStorage.getItem(STORAGE_KEY);

//   if (themeStatus !== null && themeStatus === Theme.DARK) {
//     document.body.className = themeStatus;
//   }
// }
